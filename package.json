{
  "name": "local-media-to-text",
  "version": "0.1.0",
  "private": true,
  "description": "Local scripts for converting video, links, audio, and images to text.",
  "main": "scripts/to_text.js",
  "scripts": {
    "to-text": "node scripts/to_text.js",
    "start": "node scripts/to_text.js",
    "sync": "bash scripts/sync_github.sh",
    "sync:watch": "bash scripts/watch_sync_github.sh",
    "sync:auto:install": "bash scripts/install_auto_sync.sh",
    "sync:auto:uninstall": "bash scripts/uninstall_auto_sync.sh"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cheerio": "^1.2.0",
    "dotenv": "^17.4.2",
    "opencc-js": "^1.0.5",
    "puppeteer-core": "^24.42.0"
  }
}
